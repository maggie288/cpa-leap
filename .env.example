# Server (optional; defaults in server/index.js)
# PORT=8787
# JWT_SECRETS=your_secret_v2,old_secret_v1

# Frontend: API 请求根地址。不设时：开发环境用 http://localhost:8787/api，生产用 /api（同源）
# 生产环境若前端与后端分离部署，必须设为后端完整地址，否则 /api/* 会 404
# VITE_API_BASE=https://your-backend.example.com/api
